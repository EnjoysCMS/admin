{% extends "@a/layout.twig" %}
{% block content %}
    <section class="content">


        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Редактирование пользователя {{ username }}</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <div>
                    <a class="btn btn-info btn-sm mb-3" href="{{ url('admin/user/changepassword', {"id": user.getId()}) }}">
                        Сменить пароль
                    </a>
                </div>
                {{ form.render()|raw }}
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->

    </section>
{% endblock %}