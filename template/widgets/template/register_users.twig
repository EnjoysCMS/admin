<div class="small-box bg-{{ widget.options.background.value }} h-100 d-flex flex-column justify-content-between">
    <div class="inner">
        <h3>{{ countUsers }}</h3>
        <p>{{ widget.options.title.value ?? widget.options.title ?? widget.name }}</p>
    </div>
    <div class="icon">
        <i class="ion ion-person-add"></i>
    </div>
    <a href="{{ path('admin/users') }}" class="small-box-footer">Подробнее <i class="fas fa-arrow-circle-right"></i></a>
</div>
