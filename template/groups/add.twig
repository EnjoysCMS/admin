{% extends "@a/layout.twig" %}
{% block content %}
    <section class="content">


        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Добавление группы</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                {{ form.output|raw }}
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->

    </section>
    <script>

        console.log(
            function(){
                return ((new URLSearchParams(window.location.search)).set("by", this.value)).toString();
            }
        );
    </script>
{% endblock %}
